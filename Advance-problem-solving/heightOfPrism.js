let radius=3, perimeter=15, volume=270;
let h;

// let semiperimeter, area;
//  radius= area/semiperimeter;
//  area=radius*semiperimeter;
//  semiperimeter=1/2*perimeter;
//  area=radius*1/2*perimeter;
//  volume=area*h;
//  volume=radius*1/2*perimeter*h;

 h=(2*volume)/(radius*perimeter);
 console.log(`the height of the prism is ${h}`);

